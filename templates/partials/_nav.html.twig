<nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">
    <div class="container-fluid">
        <a class="navbar-brand" href={{path('app_home')}}>Conciergerie <span class="span-logo">76</span></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03"
            aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href={{path("app_services")}}>Services</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href={{path("app_map")}}>Map</a>
                </li>


                {# User #}
                {% if app.user %}
                <ul class="navbar-nav">
                    {# <li class="nav-item">
                        <a class="nav-link" href="{{ path(" app_home") }}">Rechercher un service</a>
                    </li> #}

                    <li style="color: gray;" class="nav-item">
                        {# Hello, {{ app.user.email }} #}
                        <a class="nav-link" href="{{ path('app_user_show', {'id': app.user.id}) }}">Mon compte</a>

                    </li>

                    <li class="nav-item">
                        <a type="button" class="btn btn-dark" href="{{ path("app_logout") }}">Logout</a>
                    </li>
                </ul>

                {% else %}
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path(" app_login") }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a type="button" class="btn btn-dark" href="{{ path(" app_register") }}">Inscription</a>
                    </li>
                </ul>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>